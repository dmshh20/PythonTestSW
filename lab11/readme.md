# Звіт до роботи — Lab11: Числові методи в Python (NumPy)

Мета роботи: Навчитись використовувати бібліотеку NumPy у Jupyter Notebook для роботи з векторами та матрицями, дослідити методи arange і linspace, виконати домашні завдання (позначені >>>>> в ноутбуці) та показати результати.

---

Як виконати:
1. Перейдіть у папку:
   cd d:\pythonTestSW\PythonTestSW\lab11
2. (опціонально) Створіть віртуальне середовище і встановіть numpy:
   python -m venv .venv
   .venv\Scripts\activate
   pip install numpy
3. Відкрийте ноутбук у Jupyter:
   jupyter notebook numerical_methods.ipynb
4. Виконайте всі ячейки, відредагуйте/заповніть функції homework, збережіть вивід у notebook або скопіюйте результати у звіт.

Що зроблено в ноутбуці (фрагменти):
- Пояснення arange і linspace з прикладами;
- Функції homework1..homework4, що позначають завдання (виводять >>>>>), їх потрібно заповнити;
- Приклади створення векторів/матриць, вивід shape, арифметичні операції.

Чеклист для звіту:
- Скріншоти або текстовий вивід виконаних ячеек.
- Відповіді/пояснення: формули arange і linspace, коли застосовувати кожну.
- Результати виконання homework (вставити код і вивід).
- Приклади створених векторів/матриць і результати операцій (shape, addition, dot, transpose).

Підказка для AI (попросіть ChatGPT):
```
Explain the difference between numpy.arange and numpy.linspace, give formulas and 3 short examples for each. Also write a short explanation how numpy broadcasting works.
```