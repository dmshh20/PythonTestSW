# Звіт до роботи — Lab12: Візуалізація результатів (matplotlib)

Мета роботи: Навчитись використовувати бібліотеку matplotlib у Jupyter Notebook для побудови графіків (line, scatter, histogram, subplots), збереження фігур та пояснити результати.

--- 
Як виконати:
1. Перейдіть в папку:
   cd d:\pythonTestSW\PythonTestSW\lab12
2. (опціонально) Створіть віртуальне середовище і встановіть matplotlib:
   python -m venv .venv
   .venv\Scripts\activate
   pip install matplotlib numpy
3. Відкрийте ноутбук:
   jupyter notebook visualization.ipynb
4. Виконайте всі ячейки, допишіть реалізації у функціях homework1..homework4 (вони виводять >>>>> як позначку завдання), збережіть результати.

Що має бути виконано в ноутбуці:
- Імпорт бібліотек (matplotlib, numpy).
- Line plot з np.linspace, пояснення осі та легенди.
- Scatter plot, histogram та subplot — приклади.
- Збереження фігури у файл (png).
- Функції homework1..homework4, які ви повинні доповнити — вставте вивід у звіт.

Чеклист для звіту:
- Скріншоти або текстовий вивід виконаних ячеек.
- Короткі пояснення чому графік виглядає так (наприклад вплив масштабу, щільність точок).
- Вставлений код реалізацій homework1..homework4 та їхній вивід.
- Збережений файл з графіком (наприклад figure_line.png).

Підказка для AI (приклад prompt для ChatGPT):
```
Explain briefly (3 short paragraphs) how to choose between line, scatter and histogram plots for data exploration and provide 3 short runnable matplotlib examples.
```
